const fs = require('fs')
const path = require('path')
const parser = requrie('@babel/parser')
const traverse = require('@babel/traverse').default
const babel = require('@babel/core')

function moduleAnalyser(filename) {
    // 读取文件内容
    const content = fs.readFile
}

